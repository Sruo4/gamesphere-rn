{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    // 启用严格模式
    "strict": true,
    // 模块解析的路径映射配置
    "paths": {
      "@/*": [
        "./*"
      ]
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts"
  ]
}
